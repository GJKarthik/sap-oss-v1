[project]
name = "ainuc-be-log-local-models"
version = "0.1.0"
description = "High-performance LLM inference kernels for local model serving"
license = "MIT"
authors = ["NucleusAI Team"]
channels = ["conda-forge", "modular"]

[dependencies]
max = ">=25.1.0"

[system-requirements]
libc = ">=2.34"