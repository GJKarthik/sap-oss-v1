import type { GeneratorOptions, ParsedGeneratorOptions } from './options';
/**
 * Main entry point for the OpenAPI client generation.
 * Generates models and API files.
 * @param options - Options to configure generation.
 * @returns A promise to void.
 */
export declare function generate(options: GeneratorOptions & {
    config?: string;
}): Promise<void>;
/**
 * Main entry point for the OpenAPI client generation.
 * Generates models and API files.
 * @param options - Options to configure generation.
 * @internal
 */
export declare function generateWithParsedOptions(options: ParsedGeneratorOptions): Promise<void>;
