"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.packageJson = packageJson;
const util_1 = require("@sap-cloud-sdk/util");
const internal_1 = require("@sap-cloud-sdk/generator-common/internal");
/**
 * Generate the package.json for an openapi client so it can be released as an npm module.
 * @param options - Options to generate the package.json
 * @returns The package.json contents.
 * @internal
 */
function packageJson(options) {
    // Determine module type based on generateESM option
    const moduleType = options.generateESM ? 'esm' : 'commonjs';
    return (JSON.stringify(Object.assign(Object.assign({}, (0, internal_1.packageJsonBase)(Object.assign(Object.assign({}, options), { moduleType }))), { files: ['**/*.js', '**/*.js.map', '**/*.d.ts', '**/d.ts.map'], scripts: {
            compile: 'npx tsc'
        }, dependencies: {
            '@sap-cloud-sdk/openapi': `^${options.sdkVersion}`
        }, devDependencies: {
            typescript: '~4.1.2'
        } }), null, 2) + util_1.unixEOL);
}
//# sourceMappingURL=package-json.js.map