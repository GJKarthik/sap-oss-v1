# NVIDIA Model Optimizer Requirements for T4 GPU
# Python 3.10+ required

# Core ML frameworks
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
safetensors>=0.4.0

# Tokenizers and data handling
sentencepiece>=0.1.99
tokenizers>=0.15.0
datasets>=2.16.0

# NVIDIA specific (installed separately via setup.sh)
# nvidia-modelopt[all] - installed from NVIDIA PyPI

# Utilities
numpy>=1.24.0
tqdm>=4.66.0
pyyaml>=6.0.1
click>=8.1.0

# For calibration datasets
huggingface-hub>=0.20.0

# Optional: For exporting to TensorRT-LLM
# tensorrt-llm  # Install separately if needed

# Development and testing
pytest>=7.4.0