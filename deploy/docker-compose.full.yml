# ============================================================================
# SAP AI Fabric - FULL STACK DEPLOYMENT
# ============================================================================
# Complete deployment of all tiers in dependency order
# Usage: docker-compose -f docker-compose.full.yml up -d
# ============================================================================

version: '3.8'

services:
  # ═══════════════════════════════════════════════════════════════════════════
  # TIER 0: INFRASTRUCTURE
  # ═══════════════════════════════════════════════════════════════════════════

  elasticsearch:
    image: elasticsearch:8.12.0
    container_name: sap-ai-elasticsearch
    ports:
      - "${ES_PORT:-9200}:9200"
      - "9300:9300"
    environment:
      - node.name=${ES_NODE_NAME:-es-node-1}
      - cluster.name=${ES_CLUSTER_NAME:-sap-ai-fabric}
      - discovery.type=${ES_DISCOVERY_TYPE:-single-node}
      - xpack.security.enabled=false
      - xpack.security.enrollment.enabled=false
      - ES_JAVA_OPTS=${ES_JAVA_OPTS:--Xms512m -Xmx512m}
      - bootstrap.memory_lock=true
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - es-data:/usr/share/elasticsearch/data
    labels:
      sap.ai.tier: "tier0-infrastructure"
      sap.ai.service: "elasticsearch"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: sap-ai-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    labels:
      sap.ai.tier: "tier0-infrastructure"
      sap.ai.service: "redis"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  api-gateway:
    image: nginx:alpine
    container_name: sap-ai-gateway
    ports:
      - "8000:80"
      - "8443:443"
    volumes:
      - ../nginx.conf:/etc/nginx/nginx.conf:ro
    labels:
      sap.ai.tier: "tier0-infrastructure"
      sap.ai.service: "api-gateway"
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - agent-router
      - odata-schema
      - vllm
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  # ═══════════════════════════════════════════════════════════════════════════
  # TIER 1: MCP SERVERS
  # ═══════════════════════════════════════════════════════════════════════════

  odata-vocabularies-mcp:
    build:
      context: ../src/data/odata-vocabularies-main
      dockerfile: Dockerfile
    container_name: sap-ai-odata-mcp
    ports:
      - "${ODATA_MCP_PORT:-9150}:9150"
    environment:
      - PORT=9150
      - HOST=${ODATA_MCP_HOST:-0.0.0.0}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - ../src/data/odata-vocabularies-main/vocabularies:/app/vocabularies:ro
    labels:
      sap.ai.tier: "tier1-mcp-servers"
      sap.ai.service: "odata-vocabularies-mcp"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9150/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  odata-schema:
    build:
      context: ../src/data/odata-vocabularies-main
      dockerfile: Dockerfile
    container_name: sap-ai-odata-schema
    ports:
      - "${ODATA_SCHEMA_PORT:-8003}:8003"
    environment:
      - PORT=8003
    volumes:
      - ../src/data/odata-vocabularies-main/vocabularies:/app/vocabularies:ro
    labels:
      sap.ai.tier: "tier1-mcp-servers"
      sap.ai.service: "odata-schema"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  elasticsearch-mcp:
    build:
      context: ../src/intelligence/elasticsearch-main
      dockerfile: mcp_server/Dockerfile
    container_name: sap-ai-es-mcp
    ports:
      - "${ES_MCP_PORT:-9120}:9120"
    environment:
      - PORT=9120
      - HOST=${ES_MCP_HOST:-0.0.0.0}
      - ES_HOST=elasticsearch
      - ES_PORT=9200
      - ES_USERNAME=${ES_USERNAME:-}
      - ES_PASSWORD=${ES_PASSWORD:-}
      - ES_SCHEME=${ES_SCHEME:-http}
      - AICORE_CLIENT_ID=${AICORE_CLIENT_ID}
      - AICORE_CLIENT_SECRET=${AICORE_CLIENT_SECRET}
      - AICORE_AUTH_URL=${AICORE_AUTH_URL}
      - AICORE_BASE_URL=${AICORE_BASE_URL}
      - AICORE_RESOURCE_GROUP=${AICORE_RESOURCE_GROUP:-default}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    labels:
      sap.ai.tier: "tier1-mcp-servers"
      sap.ai.service: "elasticsearch-mcp"
    depends_on:
      elasticsearch:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9120/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  langchain-hana-mcp:
    build:
      context: ../src/data/langchain-integration-for-sap-hana-cloud-main
      dockerfile: Dockerfile
    container_name: sap-ai-langchain-hana-mcp
    ports:
      - "${LANGCHAIN_HANA_MCP_PORT:-9160}:9160"
    environment:
      - PORT=9160
      - HOST=${LANGCHAIN_HANA_MCP_HOST:-0.0.0.0}
      - HANA_HOST=${HANA_HOST}
      - HANA_PORT=${HANA_PORT:-443}
      - HANA_USER=${HANA_USER}
      - HANA_PASSWORD=${HANA_PASSWORD}
      - HANA_SCHEMA=${HANA_SCHEMA:-PAL_STORE}
      - AICORE_CLIENT_ID=${AICORE_CLIENT_ID}
      - AICORE_CLIENT_SECRET=${AICORE_CLIENT_SECRET}
      - AICORE_AUTH_URL=${AICORE_AUTH_URL}
      - AICORE_BASE_URL=${AICORE_BASE_URL}
      - AICORE_RESOURCE_GROUP=${AICORE_RESOURCE_GROUP:-default}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    labels:
      sap.ai.tier: "tier1-mcp-servers"
      sap.ai.service: "langchain-hana-mcp"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9160/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  agent-router:
    build:
      context: ../src/data/mangle-query-service
      dockerfile: Dockerfile
    container_name: sap-ai-agent-router
    ports:
      - "${AGENT_ROUTER_PORT:-8010}:8010"
    environment:
      - PORT=8010
      - ELASTICSEARCH_URL=http://elasticsearch:9200
      - ODATA_SERVICE_URL=http://odata-schema:8003
      - VLLM_URL=http://vllm:8080
      - HANA_URL=${HANA_URL}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    labels:
      sap.ai.tier: "tier1-mcp-servers"
      sap.ai.service: "agent-router"
    depends_on:
      elasticsearch:
        condition: service_healthy
      odata-schema:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8010/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  # ═══════════════════════════════════════════════════════════════════════════
  # TIER 2: INTELLIGENCE LAYER
  # ═══════════════════════════════════════════════════════════════════════════

  vllm:
    build:
      context: ../src/intelligence/vllm-main
      dockerfile: Dockerfile.qwen3-q8
    container_name: sap-ai-vllm
    ports:
      - "${VLLM_PORT:-8080}:8080"
    environment:
      - PORT=8080
      - HOST=${VLLM_HOST:-0.0.0.0}
      - MODEL_PATH=${VLLM_MODEL_PATH:-/app/models}
      - MODEL_NAME=${VLLM_MODEL_NAME:-Qwen/Qwen2.5-7B-Instruct-GGUF}
      - CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES:-0}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - model-cache:/app/models
    labels:
      sap.ai.tier: "tier2-intelligence"
      sap.ai.service: "vllm-inference"
      sap.ai.gpu: "required"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 120s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  ai-core-pal:
    build:
      context: ../src/intelligence/ai-core-pal
      dockerfile: Dockerfile
    container_name: sap-ai-core-pal
    ports:
      - "${AICORE_PAL_PORT:-9881}:8080"
    environment:
      - PORT=8080
      - HOST=${AICORE_PAL_HOST:-0.0.0.0}
      - HANA_HOST=${HANA_HOST}
      - HANA_PORT=${HANA_PORT:-443}
      - HANA_USER=${HANA_USER}
      - HANA_PASSWORD=${HANA_PASSWORD}
      - HANA_URL=${HANA_URL}
      - PRIVATE_LLM_URL=http://vllm:8080
      - VLLM_URL=http://vllm:8080
      - MANGLE_ROUTER_URL=http://agent-router:8010
      - AICORE_CLIENT_ID=${AICORE_CLIENT_ID}
      - AICORE_CLIENT_SECRET=${AICORE_CLIENT_SECRET}
      - AICORE_AUTH_URL=${AICORE_AUTH_URL}
      - AICORE_BASE_URL=${AICORE_BASE_URL}
      - AICORE_RESOURCE_GROUP=${AICORE_RESOURCE_GROUP:-default}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    labels:
      sap.ai.tier: "tier2-intelligence"
      sap.ai.service: "ai-core-pal"
    depends_on:
      vllm:
        condition: service_healthy
      agent-router:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  genai-toolkit:
    build:
      context: ../src/intelligence/generative-ai-toolkit-for-sap-hana-cloud-main
      dockerfile: Dockerfile
    container_name: sap-ai-genai-toolkit
    ports:
      - "${GENAI_TOOLKIT_PORT:-8084}:8084"
    environment:
      - PORT=8084
      - HOST=${GENAI_TOOLKIT_HOST:-0.0.0.0}
      - HANA_HOST=${HANA_HOST}
      - HANA_PORT=${HANA_PORT:-443}
      - HANA_USER=${HANA_USER}
      - HANA_PASSWORD=${HANA_PASSWORD}
      - HANA_URL=${HANA_URL}
      - VLLM_URL=http://vllm:8080
      - MANGLE_ROUTER_URL=http://agent-router:8010
      - AICORE_CLIENT_ID=${AICORE_CLIENT_ID}
      - AICORE_CLIENT_SECRET=${AICORE_CLIENT_SECRET}
      - AICORE_AUTH_URL=${AICORE_AUTH_URL}
      - AICORE_BASE_URL=${AICORE_BASE_URL}
      - AICORE_RESOURCE_GROUP=${AICORE_RESOURCE_GROUP:-default}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    labels:
      sap.ai.tier: "tier2-intelligence"
      sap.ai.service: "genai-toolkit"
    depends_on:
      vllm:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8084/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  embedded-hana:
    build:
      context: ../src/data/langchain-integration-for-sap-hana-cloud-main
      dockerfile: Dockerfile
    container_name: sap-ai-embedded-hana
    ports:
      - "${EMBEDDED_HANA_PORT:-8050}:8050"
    environment:
      - PORT=8050
      - HANA_URL=${HANA_URL}
      - VLLM_URL=http://vllm:8080
      - MANGLE_ROUTER_URL=http://agent-router:8010
      - AICORE_CLIENT_ID=${AICORE_CLIENT_ID}
      - AICORE_CLIENT_SECRET=${AICORE_CLIENT_SECRET}
      - AICORE_AUTH_URL=${AICORE_AUTH_URL}
      - AICORE_BASE_URL=${AICORE_BASE_URL}
      - AICORE_RESOURCE_GROUP=${AICORE_RESOURCE_GROUP:-default}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    labels:
      sap.ai.tier: "tier2-intelligence"
      sap.ai.service: "embedded-hana"
    depends_on:
      vllm:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8050/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - sap-ai-fabric-network
    restart: unless-stopped

  # ═══════════════════════════════════════════════════════════════════════════
  # TIER 3: TRAINING (Optional - uncomment to include)
  # ═══════════════════════════════════════════════════════════════════════════

  # modelopt-api:
  #   build:
  #     context: ../src/training/nvidia-modelopt
  #     dockerfile: Dockerfile
  #   container_name: sap-ai-modelopt-api
  #   ports:
  #     - "${MODELOPT_API_PORT:-8001}:8001"
  #   environment:
  #     - PORT=8001
  #     - HOST=${MODELOPT_API_HOST:-0.0.0.0}
  #     - OUTPUT_DIR=${MODELOPT_OUTPUT_DIR:-/app/outputs}
  #     - LOG_LEVEL=${LOG_LEVEL:-INFO}
  #   volumes:
  #     - model-cache:/app/models
  #     - modelopt-outputs:/app/outputs
  #   labels:
  #     sap.ai.tier: "tier3-training"
  #     sap.ai.service: "modelopt-api"
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   healthcheck:
  #     test: ["CMD", "curl", "-sf", "http://localhost:8001/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #   networks:
  #     - sap-ai-fabric-network
  #   restart: unless-stopped

volumes:
  es-data:
    driver: local
  redis-data:
    driver: local
  model-cache:
    driver: local
  # modelopt-outputs:
  #   driver: local

networks:
  sap-ai-fabric-network:
    name: ${NETWORK_NAME:-sap-ai-fabric-network}
    driver: ${NETWORK_DRIVER:-bridge}